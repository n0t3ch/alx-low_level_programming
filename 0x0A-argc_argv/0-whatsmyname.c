#include <stdio.h>

/**
 * main - print program name
 * @argc: argument count
 * @argv: argument array
 * Return: 0 on success
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
